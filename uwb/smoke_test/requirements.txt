# Runtime dependencies for online Aorta checks and standalone serial/BLE helpers.
# The sensor_tools subdirectory ships with this folder; no root project install is needed.
mcap>=1.2
PyYAML>=6
pyserial>=3.5
bleak>=0.22
