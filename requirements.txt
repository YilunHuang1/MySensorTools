# Run from the repository root. Installs the shared readers and analysis tools.
-e .[analysis,device,infrared]

# Optional interactive point-cloud viewer: pip install open3d
# Linux BLE peripheral mode additionally requires OS-provided dbus-python/PyGObject.
